title:               Zettelgarden - A Second Brain in public
tagline:            Zettelkasten style Digital Garden created by Rahul Rajeev
url:                https://garden.rahulrajeev.net
description: An experimental Zettelkasten style Digital Garden created and maintained by Rahul Rajeev as his Second Brain to facilitate Thinking in Public.
twitter:
  username: rahulrajeeev
  card: summary
logo: /favicon-32x32.png
locale: eng_UK
social:
  name: Rahul Rajeev
  links:
    - https://polywork.com/rahulrajeev
    - https://github.com/drawablex86
include:             ['_pages']
exclude:             ['_includes/notes_graph.json']
# You may need to change the base URL depending on your deploy configuration.
# Specifically, when using GitHub Pages, the baseurl should point to where GitHub
# Pages deploys your repository (which is usually the repository name).
baseurl:             ''

# If you are using a host that cannot resolve URLs that do
# not end with .html (such as Neocities), set this to 'true'.
use_html_extension:  false

# Set to `true` to open non-internal links in new tabs, or
# set to `false` to open non-internal links in current tab.
open_external_links_in_new_tab: true

# Set to `true` to replace tweet URLs with Twitter embeds.
# Note that doing so will negatively the reader's privacy
# as their browser will communicate with Twitter's servers.
embed_tweets: false

permalink:           pretty
relative_permalinks: false

plugins:
  - jekyll-last-modified-at


sass:
  sass_dir:          _sass
  style:            :compressed

collections:
  notes:
    output: true
    permalink: /:slug

defaults:
  - scope:
      path: "**/*"
    values:
      layout: "default"
  - scope:
      path: "_notes/**/*.md"
    values:
      layout: "note"

#Google Analytics
google_analytics: UA-204964000-1




